import React from "react";
import './Print.scss'
import {useParams} from "react-router-dom";

export const Print = () => {
    const {id} = useParams();

    return (
        <div className="Print">

        </div>
    )
}
